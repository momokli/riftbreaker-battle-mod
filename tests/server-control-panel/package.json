{
  "name": "server-control-panel",
  "version": "1.0.0",
  "private": true,
  "description": "Web-UI-Test fuer das Cockpit-Panel server control (Plane B, Issue #422): defensives Rendering + POST-Routen. Keine Dependencies, kein Netzwerk.",
  "scripts": {
    "test": "node --test"
  }
}
