{
  "name": "rbbattle-e2e-vollkette",
  "version": "1.0.0",
  "private": true,
  "description": "rbbridge host-test: kompiliert die Scan-/RTTI-Funktionen aus rbbridge.c gegen einen synthetischen PE-Puffer (kein Windows/Spiel).",
  "scripts": {
    "test": "node --test"
  }
}
