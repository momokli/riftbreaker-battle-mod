---
# Rolle: dedicated-server-image (Issue #241) — baut rb-dedicated:<deploy-sha>
# auf dem Zielhost. Das Image ist die Wine-Laufzeit für :6321
# (Community-Rezept j3n5-group/riftbreaker-docker, vendored unter
# tools/dedicated-server/). Tag = Deploy-SHA (kein `latest`).

# Staging-Verzeichnis für den Build-Kontext auf dem Zielhost. Der Kontext wird
# aus dem Repo-Checkout (tools/dedicated-server) dorthin synchronisiert.
dedicated_server_context_dir: /opt/rbmods/dedicated-server
