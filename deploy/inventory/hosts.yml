---
# Ansible-Inventory für den Deployment-Host "planet" (Hetzner).
#
# Mesh-first: SSH läuft über den Alias "planet" aus ~/.ssh/config (Tailscale,
# 100.77.143.105) — NIEMALS über die Public-IP (65.21.27.234 ist nur für
# Services). Kein Deploy ohne explizite Freigabe.

all:
  hosts:
    planet:
      ansible_user: root
      ansible_connection: ssh
